export const BRAND = {
  name: "SignalNest",
  tagline: "Queue-free monitoring",
  description:
    "A calm, branded workspace for checking bureaucratic portals, delivery queues, and slow status pages without babysitting tabs."
};

export const PROVIDER_SPOTLIGHT = {
  title: "CGSudan passport arrival monitor",
  copy:
    "The first provider watches the Sudan consulate passport page in Dubai, detects the true status message, and records changes for desktop alerts or future channels.",
  bullets: [
    "Uses the actual page interaction instead of brittle string matching alone.",
    "Keeps job state so identical runs stay quiet.",
    "Can be triggered from the UI today and a cron runner next."
  ]
};
